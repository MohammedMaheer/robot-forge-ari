{
  "name": "@robotforge/processing-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": ".venv\\Scripts\\python -m uvicorn main:app --reload --host 0.0.0.0 --port 8002",
    "build": "echo 'Python service — no JS build'",
    "lint": "ruff check .",
    "typecheck": "pyright ."
  }
}
